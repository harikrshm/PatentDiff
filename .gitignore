# Python
__pycache__/
*.pyc
*.pyo

# Environment / secrets
.env
.venv/
venv/

# Dash / worktrees / caches
.dash_cache/
.worktrees/
.claude/worktrees/
.superpowers/
.pytest_cache/

# Installed skill bundles (restorable via skills-lock.json)
.agents/
.claude/skills/

# Trace data (large generated artifacts) — keep curated manifests only
traces/*.jsonl
!traces/experiments.jsonl
traces/workbench_state/
traces/*.xlsx
traces/*.log
traces/*_run_ids.txt
traces/*_ids.txt
traces/kpi_targets.json
